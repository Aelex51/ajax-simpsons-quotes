// This function loads pokemon data from the Pokemon API
function fetchsimpsonsQuoteJSON() {
  // Feel free to download this HTML and edit it, to use another Pokemon ID
  const quote = 'quote';
  const url = `https://simpsons-quotes-api.herokuapp.com/quotes${quote}`;
  axios.get(url)
    .then(function(response) {
      return response.data; // response.data instead of response.json() with fetch
    })
    .then(function(simpsonsQuote) {
      console.log('data decoded from JSON:', simpsonsQuote);

      // Build a block of HTML
      const simpsonsHtml = `
        <p><strong>${quotes.quote}</strong></p>
         <img src="${quotes.sprites.front_shiny}" />`;
      document.querySelector('#chuck-norris').innerHTML = simpsonsHtml;
    });
}
fetchsimpsonsQuoteJSON()

<img src="${quote.sprites.front_shiny}" />;